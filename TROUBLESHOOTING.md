# 트러블슈팅

## 1. 모든 화면이 1~2초씩 느린 문제 — `localhost`의 IPv6 폴백 지연 (2026-09-14)

- **증상**: 로그인, 관리자 모드 전환, 교육생 목록 조회, 출석현황 조회가 각각 약 1~2초,
  얼굴 인식 입실/퇴실은 약 7초가 걸림. 등록된 교육생은 2명뿐이라 데이터량 문제가 아님.

- **원인**: `localhost`는 IPv6(`::1`)와 IPv4(`127.0.0.1`) 양쪽으로 해석되고 Windows는
  IPv6를 먼저 시도한다. 그런데 이 프로젝트의 자체 소켓 서버들은 **IPv4로만 리스닝**한다.

  ```
  netstat -ano
  3306 (MySQL)      0.0.0.0:3306 + [::]:3306   <- 양쪽 다 리스닝
  5080 (C# 서버)     0.0.0.0:5080              <- IPv4만
  8001 (AI 서버)     0.0.0.0:8001              <- IPv4만
  ```

  `server/Program.cs`는 `TcpListener(IPAddress.Any, ...)`, `ai-server/main.py`는
  `0.0.0.0`에 바인딩하는데 둘 다 IPv4 전용이다. 따라서 `::1:5080`으로의 접속 시도는
  받아주는 리스너가 없어 SYN이 그대로 드롭되고, **약 2초 타임아웃 후에야** IPv4로
  폴백해 성공한다. 요청마다 새 TCP 연결을 맺는 구조라 이 2초가 **매 요청마다** 발생했다.

- **실측 (실행 중인 C# 서버에 동일 요청을 3회씩)**

  | 요청 | `localhost` 경유 | `127.0.0.1` 경유 |
  |---|---|---|
  | login | 연결 2036~2071ms + 처리 129ms = **약 2.2초** | 연결 0~17ms + 처리 129ms = **약 0.13초** |
  | getStudents | 연결 2055~2060ms + 처리 4ms = **약 2.06초** | 연결 13ms + 처리 3ms = **약 0.016초** |
  | getAttendanceSummary | 연결 약 2056ms + 처리 6ms = **약 2.06초** | 연결 12ms + 처리 4ms = **약 0.017초** |

  연결 시간만 놓고 보면 **약 2040ms → 약 10ms**로 줄어든다.

- **조치**: 호스트명 대신 IP를 직접 지정하도록 변경.
  - `client/Services/Config.cs` — `ServerHost` 기본값 (WPF → C# 서버)
  - `server/Config.cs` — `AiServerHost` 기본값 (C# 서버 → AI 서버), `DbConnectionString`
  - `ai-server/config.py` — `DB_HOST` 기본값
  - 환경변수(`ATTENDANCE_SERVER_HOST`, `ATTENDANCE_AI_HOST`, `ATTENDANCE_DB_HOST`,
    `ATTENDANCE_DB_CONNECTION`)로는 여전히 덮어쓸 수 있음.

  > 대안: 서버를 `IPAddress.IPv6Any` + `DualMode`로 바인딩해 IPv6/IPv4 양쪽을 받게 하면
  > `localhost`를 그대로 써도 된다. 다만 접속 대상을 IP로 고정하는 쪽이 더 단순하고
  > 예측 가능해서 위 방법을 택했다.

- **얼굴 인식 7초에 대한 영향**: 입실/퇴실 1회는 `WPF → C# 서버`와 `C# 서버 → AI 서버`
  **두 번의 연결**을 맺으므로 이 지연을 2회(약 4초) 물고 있었다. 위 수정으로 약 4초가
  사라지고, 남는 약 3초가 실제 AI 추론 비용이다(아래 2번 항목).

- **진단 과정에서 한 번 잘못 기각했던 이유 (기록용)**:
  이 가설을 처음 세웠을 때 MySQL(3306)을 대상으로 측정해서 차이가 없다는 결과를 얻고
  가설을 폐기했었다. 그런데 MySQL은 IPv4/IPv6 양쪽을 모두 리스닝하기 때문에 `::1`
  접속이 즉시 성공해 애초에 이 현상이 나타나지 않는 서비스였다. **증상이 나타나는
  포트(5080)를 직접 대상으로 측정**하고 나서야 재현됐다. 성능 가설은 반드시 문제가
  발생하는 실제 대상에 대해 검증해야 한다.

## 2. 얼굴 인식(입실/퇴실) AI 추론 비용

- **증상**: 위 1번 수정 이후에도 인식에 약 3초가 걸림.

- **원인**: `client/Views/AttendanceCheckView.xaml.cs`의 `CheckFrameCount = 5`로 5장을
  캡처해 보내면, `ai-server/app.py`의 `handle_recognize`가 **5장 전부**에 대해
  `DeepFace.represent()`(MTCNN 얼굴검출 + ArcFace 임베딩추출)를 수행한다.
  MTCNN은 3단계 CNN 캐스케이드라 CPU에서 프레임당 수백 ms가 든다.
  게다가 첫 프레임에서 확실하게 매칭돼도 **나머지 프레임을 모두 처리한 뒤**에야 응답한다
  (조기 종료 없음).

- **개선 후보** (아직 미적용):
  | 방법 | 효과 | 트레이드오프 |
  |---|---|---|
  | 조기 종료 (임계값+마진 충족 시 즉시 중단) | 최대 5배 | "5장 중 최고점" → "처음 충분히 좋은 것" 으로 선택 기준이 바뀜 |
  | 검출기를 `mtcnn` → `yunet`/`ssd` 로 교체 | 2~5배 | 정확도 재검증 필요 |
  | 프레임 수 5 → 3 | 40% 감소 | 인식 실패율 소폭 상승 가능 |
  | 시작 시 모델 사전 로딩 | 첫 요청 지연 제거 | 서버 기동이 느려짐 |

## 3. 참고: 로그인 처리 시간의 구성

원인 추적 당시 임시로 넣었던 타이밍 로그로 측정한 값이다(로그는 원인 파악 후 제거함).
로그인 요청의 서버 처리 시간 약 130ms 중 `bcrypt.Verify`가 약 129ms를 차지한다.
BCrypt는 의도적으로 느리게 설계된 해시 함수이므로 정상 동작이며, 줄이려고 하면 안 된다.

## 4. 동작 확인 절차

### 준비 상태별로 확인할 수 있는 범위

| 준비 상태 | 확인 가능 |
|---|---|
| 클라이언트만 실행 | 로그인 화면 표시, 빈 입력 안내 |
| MySQL + 메인 서버 + 클라이언트 | 로그인, 모드 전환, 교육생 검색·등록·수정·삭제, 출결 기록 조회 |
| 위 + AI 서버 + 웹캠 | 얼굴 등록, 얼굴 인식, 입실·퇴실 기록 |

캠 없이 확인할 때는 교육생 신규 등록 후 얼굴 등록 여부를 묻는 안내에서 **아니요**를 선택하고,
나중에 교육생 목록의 얼굴등록 버튼으로 촬영하면 된다.

가상 데이터를 쓰는 시연 모드나 이미지 파일 업로드 인식 기능은 없다.
따라서 실제 인식 대신 수행한 화면 검증을 얼굴 인식 성공으로 기록하지 않는다.

### 얼굴 인식 확인 순서

1. 웹캠을 연결하고 다른 프로그램이 카메라를 점유하고 있지 않은지 확인한다.
2. MySQL → AI 서버 → 메인 서버 → 클라이언트 순으로 실행한다.
3. 관리자로 로그인해 교육생을 등록한다.
4. 교육생 목록에서 **얼굴등록**을 선택하고 **촬영 시작**을 누른다. (12프레임)
5. 등록 성공을 확인한 뒤 출석 키오스크로 전환한다.
6. **웹캠 시작** → **입실**로 인식 결과를 확인한다. (5프레임)
7. **퇴실**을 눌러 처리 결과를 확인한다.
8. 관리자 비밀번호를 입력해 관리자 모드로 돌아가 해당 날짜의 출결 기록을 조회한다.

## 5. 자주 확인할 문제

| 증상 | 확인할 내용 |
|---|---|
| 서버에 연결할 수 없음 | 메인 서버 실행 여부, 서버 주소, 포트 5080, 방화벽 |
| 메인 서버 시작 실패 | MySQL 실행 여부, 스키마 적용 여부, DB 계정·비밀번호, 포트 충돌 |
| 로그인 실패 | 관리자 계정 정보와 DB 연결. 기본 계정은 `admins` 테이블이 비었을 때만 시드된다 |
| 웹캠을 열 수 없음 | 캠 연결, Windows 카메라 접근 권한, 다른 프로그램의 캠 점유 |
| 얼굴 등록·인식 요청 실패 | AI 서버 실행 여부, 메인 서버의 AI 주소·포트, Python 터미널 로그 |
| 인식 실패 또는 미등록으로 처리됨 | 해당 교육생의 얼굴 등록 여부, 조명·초점·얼굴 가림. 1위와 2위 후보가 비슷하면 의도적으로 실패 처리된다 (`MARGIN_THRESHOLD`) |
| 빌드했는데 화면이 그대로임 | 시작 프로젝트가 맞는지, 새로 빌드했는지 확인 |
| 빌드 시 exe 복사 실패 | 해당 서버가 실행 중이면 exe가 잠긴다. 종료 후 다시 빌드한다 |
| 설정을 바꿨는데 반영되지 않음 | 실행 중인 프로세스를 종료하고, 환경변수를 설정한 터미널에서 다시 실행한다 |
| 얼굴 등록은 됐는데 인식이 안 됨 | `ai-server/config.py`의 `MODEL_NAME`을 바꿨는지 확인한다. 모델이 다르면 임베딩 차원이 달라 비교 대상에서 제외되므로 재등록이 필요하다 |
